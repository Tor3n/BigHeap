package interView.frequentlyAsked_by_kirill.testClasses;

public class Rrec {
    //record is a type of class for which get(), set(), hashcode(), and equals() methods are already written
    //record recordName(int a) {
        //optional statements
    //}
    public static void main(String[] args) {

    }
}
